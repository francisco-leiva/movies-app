export default async function Search({ params }) {
  const { id } = params;

  return (
    <main>
      <section className='max-w-4xl w-full px-2 pt-32 pb-8 grid grid-cols-cards gap-x-4 gap-y-4 sm:px-0 md:mx-auto'></section>
    </main>
  );
}
